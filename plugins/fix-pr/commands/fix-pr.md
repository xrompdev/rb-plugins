---
description: Diagnose and fix failing CI checks on a GitHub pull request
argument-hint: "<PR number>"
allowed-tools:
  - Bash
  - Read
  - Edit
  - MultiEdit
  - Write
  - Grep
  - Glob
  - Agent
---

# Fix PR Failing Checks

Fix failing CI checks on the PR specified in the arguments.

**You MUST use the `fix-checks` skill to execute this task.** Invoke it now with the Skill tool, passing the PR number as the argument.
